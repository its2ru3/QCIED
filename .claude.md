# AI Behavioral Guidelines (from CLAUDE.md)

Behavioral guidelines to reduce common LLM coding mistakes. Merge with project-specific instructions as needed.

**Tradeoff:** These guidelines bias toward caution over speed. For trivial tasks, use judgment.

## 1. Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them - don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

## 2. Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

## 3. Surgical Changes

**Touch only what you must. Clean up only your own mess.**

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it - don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

The test: Every changed line should trace directly to the user's request.

## 4. Goal-Driven Execution

**Define success criteria. Loop until verified.**

Transform tasks into verifiable goals:
- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.

---

**These guidelines are working if:** fewer unnecessary changes in diffs, fewer rewrites due to overcomplication, and clarifying questions come before implementation rather than after mistakes.

# Quantum Computing Rules

- Do NOT use code from Qiskit 0.x or 1.x tutorials/StackOverflow
- Always use Qiskit >= 2.0 (latest stable)
- Use V2 primitives: `SamplerV2`, `EstimatorV2` from `qiskit.primitives`, `backend.run()` is deprecated
- Never use deprecated `execute()` — use primitives instead
- `from qiskit.tools.visualization` is gone — use `from qiskit.visualization`
- Use `qiskit.transpiler` for circuit optimization
- Import from `qiskit.circuit`, not `qiskit.extensions`
- Use `pip show qiskit` and `pip show qiskit-aer` to verify installed versions before coding
- When unsure about an API, run: python -c "import qiskit; help(qiskit.module)" or check local docs

## Project Structure
- `./Paper_markdown/` — paper content in markdown with figure descriptions
- `./helpers/` — implemented helper function modules
- `./tests/` — unit tests verifying subcircuits against paper results
- `main_4x4_simulation*.ipynb` — testing the full edge detection implementation for 4x4 image

## Paper Context
Saved in ./Paper_markdown directory in markdown format with pictures and their descriptions. Use arXiv for more details on referenced papers if needed. 

## Rules
- When there is a circuit diagram image, map each gate symbol precisely
- Standard conventions: single lines = qubit wires, double lines = classical bits
- H = Hadamard, X = Pauli-X, Z = Pauli-Z, filled dot = control, ⊕ = CNOT target
- Rz, Rx, Ry with angle = rotation gates, implement with exact parameter from paper
- Slash with number on wire = multi-qubit register (note the qubit count)
- Double vertical lines = barrier
- Boxes with labels = other known gates or named unitaries(implement as custom gates)
- Dashed boxes = subcircuits, implement as separate functions
- Always add comments mapping back to the figure number in the paper
- Verify gate count and qubit count match the diagram
- After building each subcircuit, print the circuit drawing for visual verification, in chat and code both

## Workflow
1. Read the relevant paper section for context
2. Look at the circuit diagram image
3. Implement as a Qiskit function
4. Draw the circuit with `circuit.draw('text')` so we can compare
5. Write unit tests verifying known input/output states from the paper

## Environment
- Claude Code runs on Windows (not inside WSL)
- Code is stored on Windows filesystem (accessible in WSL via /mnt/c/... or /mnt/d/...)
- Code and tests run inside WSL (Ubuntu)
- Use `wsl` prefix or `wsl -e` when running Python/pip commands from Claude Code
- All Python dependencies are installed inside WSL, not Windows
